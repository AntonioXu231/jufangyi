# PL 数据流与滤波一体化方案（V2）

> 工程：`pd_feature_bd_2020_2_ps_dsp_split_2026091`（基线 v1.4.0）
> 器件：`xc7z020clg400-2`｜ADC 26 MSPS｜PL 系统时钟 130 MHz｜Slice 上限 13,300
> 日期：2026-09-17
> **版本说明：本文件是重建版。** 原 `PL数据流与滤波一体化方案_V2_20260917.md` 位于已删除的
> 工作目录根下（该目录内含的另外两份报告同时丢失），且**从未入库 git，无法逐字恢复**。
> 本版依据下列**仍在盘上的一手材料**重建，数值与结论均取自这些文件的原文，未作新估算。

**重建溯源（全部已核对存在）**

| 材料 | 位置 | 承担的内容 |
|---|---|---|
| `PL资源不足分析与软硬件划分方案_20260917.md` | `merge/pd_feature_bd_2020_2_dds_onboard_iir_ddr1g_20260916/` | 资源缺口归因、三档方案 |
| `滤波前置与系数下发两个问题的回答_20260917.md` | `merge/pd_feature_bd_2020_2_ps_dsp_split_2026091/` | 判据可用性的定量证明 |
| `PL侧数字滤波完整解决方案_20260917.md` | 同上 | 级数裁剪（9 章，本文件 §三 主源） |
| `PL侧数字滤波与Slice优化_P0P1最终执行定稿_20260917.md` | 同上 | **冲突时以它为准** |
| `协作状态与证据索引_20260917.md` | 同上 | 双方唯一事实索引（E-01～E-41） |
| `P1最终方案与操作步骤_20260917.md` | 同上 | 手工分段 Tcl（阶段 A–E） |
| `.workbuddy/memory/2026-09-17.md` | 工作区 | 本轮全部推导与实测原文 |

> **不能复原的部分**：原文件附带的 4 张可视化图（单链 vs 单点扇出对比图 / 推荐数据流全景图 /
> PS 侧耗时对数条形图 / 四阶段路线与 P1 门禁图）为会话内渲染，未落盘，本版以表格替代。

---

## 一、与既有文档的关系

本文件**不新增任何技术结论**，只把上表七份材料合成**一份可执行口径**，解决三个问题：

1. **数据流**到底该是单链还是多支？（§二）
2. **滤波**保留什么、砍什么、凭什么砍？（§三）
3. **先做哪一步**、怎么判通过、什么条件下回退？（§四–§六）

三处以上材料之间的口径已统一，凡有出入以列出的依据为准：

- `N_BP/N_NT` **仅是节数计数参数，不表示带通或陷波类型**——滤波类型只由下发的
  `b0,b1,b2,a1,a2` 系数决定（定稿 L110）。本文件统一取 `N_BP=1, N_NT=0`。
- "before/after 必须落在同一工程目录"（见 §五）。
- `PL资源不足分析` 末尾"65 MSPS 下无论如何都无法在 PL 内实现"的表述已收窄为
  **"不加抽取时成立"**（§九）。

---

## 二、定案一：数据流 —— 一次采集 · 单点扇出 · 三支并行

### 2.1 结论

**"采集 → 滤波 → 提取/FFT"单链串行不是最优方案。** 原因是三种需求互相冲突，绑在一条链上
必然在某处打架：

| 需求支路 | 对信号的要求 | 与其它支路的冲突 |
|---|---|---|
| **判决（实时触发）** | 去工频、低延迟、窄带 | 与"分析"冲突：窄带会毁掉全频带 |
| **存档（快照）** | 无损原始、可重放 | 与"判决"冲突：它要的就是未处理的数据 |
| **分析（频谱）** | 全频带、保相位、可重算 | 与"判决"冲突：滤波的非线性相位污染相位 |

**最优形态 = 一次采集、单点扇出、三支并行**，由四条判据支撑：

1. **不可逆操作后置**——滤波是有损且不可逆的，越靠后越好；
2. **需求冲突不可同链**——窄带与全频带不能共用一条通路；
3. **相位保真**——IIR 是非线性相位，不能放在相位敏感的 PRPD 之前；
4. **算力错配**——20 ms 节拍内 FFT 有 55× 余量，IIR 却超预算 3×，应当分开处理。

### 2.2 硬件骨架已经是正确的，不需要新建扇出

实测 `pd_ddr_wr_top.v`：该模块**内部已经是扇出结构**，而不是单链：

| 位置 | 内容 |
|---|---|
| L137–138 | `s48_a_data/valid`（注释 `// Path A: -> FIFO -> DDR`）、`s48_b_data/valid`（`// Path B: -> 实时算法链`） |
| L239–277 | **两个独立 FIFO**（L244–245 / L263–264，`.wr_clk=adc_clk`、`.rd_clk=clk`） |
| L227–230 | Path A 引 `o_s48_data/o_s48_valid`；Path B 引 `o_ch_data/o_ch_valid` |
| L275–276 | `assign o_feat_data = feat_fifo_dout` |
| L39–40 | 模块输入是 `adc_data/adc_dv` —— **原始数据** |

→ **扇出点位于 CDC 之后、任何有损处理之前，位置本来就是对的；写进 DDR 的本来就是原始数据。**

### 2.3 唯一需要改的东西：分析支路换位置

```
从：滤波之后（filt_data）+ PL 之内（片内 FFT）
改为：原始快照 + PS 侧（PS 读快照做 FFT）
```

单链方案的三处具体错误（这是"为什么必须改"的正面理由）：

1. **有损前置锁死下游**——滤波一旦在链首，存档就拿不到原始数据，事后无法重算；
2. **IIR 非线性相位先于 FFT，污染 PRPD 相位**——相位是局放识别的核心观测量；
3. **"IIR 带通 + FFT"把同一件事做了两遍**——两者都在做频带选择，重复付出资源。

### 2.4 定量支撑（全部以 20 ms 冻结节拍为预算）

| 任务 | 耗时 | 相对 20 ms 预算 |
|---|---|---|
| 4ch × 1024 点 FFT（PS 侧） | ≈ **0.36 ms** | **55× 余量** |
| 事件窗 1024–4096 点 IIR | 0.12–0.49 ms | 余量充足 |
| **整快照 IIR（520,000 点 × 4ch）** | **62.5 ms** | **超预算 3×，不可行** |

→ 一句话：**"FFT 便宜、IIR 贵"**。这决定了频谱分析可以搬去 PS，而实时滤波不能。

### 2.5 工作区三种拓扑并存（实测 BD 连线）

| 拓扑 | 连线 | 出现于 |
|---|---|---|
| ① 无滤波 | `pd_ddr_0/o_feat_data → pd_feature_0/adc_data` | `merge/pd_feature_bd_2020_2` |
| ② **串联** | `o_feat_data → pd_filter_0 → pd_feature_0` | v1.4.0 系 / `ps_dsp_split_2026091` / 用户工程 |
| ③ 并联 | `o_feat_data` 同时扇出到 `pd_filter_0` 与 `pd_feature_0`；`filt_data → pd_fft_input_adapter → xfft_ch0..ch3` | `merge/..._ddr1g_20260916` |

**核心洞察：② 与 ③ 只差一条连线**——即 `pd_feature_0/adc_data` 取 `pd_filter_0/filt_data`
还是 `pd_ddr_0/o_feat_data`。

因为 `pd_filter_chain` **默认旁路且位精确透传**（复位后全局旁路与四通道旁路均为 1），
**默认配置下两者行为完全等价** → `v1.4.0 = v1.5.0 + 启用滤波的选项`，**纯增量、零当前代价**
→ **选串联方向**。

> ⚠️ **不要拿拓扑解释资源差异。** ② 与 ③ 的资源差**全部来自那 4 个 FFT**，与串/并联无关。
> 并联还有一个过程事实：它当年是"改一处丢一处"——加 FFT+DDR1G 时静默取消了"滤波服务触发"
> 的连线取向，且未记入契约（脚本 `08_add_iir_notch_filter_bypass.tcl` L56–57 可证原为串联）。

### 2.6 推荐目标形态（混合）

```
CDC/raw ──┬─→ pd_ddr_0 环形写 ──→ 冻结/四槽快照 ──→ PS 侧 FFT/选频
          │                     （存原始数据，可重放、可重算）
          │
          └─→ pd_filter_0（默认旁路；启用后 1~2 节/通道）
                    │
                    ├─→ pd_feature_0   触发判据（吃滤波后数据）
                    └─→ [可选] 频谱/上位机支路
```

该形态对应契约 §2 / §8 的**原意**（"滤波由 PL 实时执行"），**唯一需要改契约的地方**是
"DDR 收滤波后数据" → 应改为"DDR 收原始数据"（见 §七）。

### 2.7 启用滤波的两个副作用（必须先知道，否则会误判为故障）

1. **数据与时间戳延后 8 个 ADC 采样 ≈ 0.31 µs**。相位原点仅平移 0.0055°（可忽略），
   但**事件时间戳索引快照会差 8 个采样**——需在软件侧补偿。
2. **滤波故障会污染触发判据**（滤波在触发链上）。缓解：旁路是安全态，
   建议补"旁路 vs 使能"统计自检，超限自动回旁路（§三 决策规则末行）。

---

## 三、定案二：滤波 —— "先定带，再选构"

### 3.1 三段职责划分（关键：把不同频段的问题交给不同手段）

| 要解决的问题 | 手段 | PL 硬件代价 |
|---|---|---|
| **工频 50 Hz**（最大干扰，90 码 ≈ 12.9σ） | **相位窗同步平均**（Bresenham 自适应分窗，`pd_feature_core.v` L171–183 / L255–307） | **0**（纯逻辑，无滤波资源） |
| **目标频带整形**（100 kHz – 1 MHz） | **1~2 节 IIR biquad** | **8~16 DSP** |
| **频谱细节 / 选频** | **PS 侧 FFT + 带外 bin 置零** | **0 PL 资源** |

**这三行是本方案能省下约 75% 资源的全部根据**：工频在相位窗内是准直流，所以它不需要
"窄带带通"来处理，一个相位窗平均就够了。

**相位窗的准确口径**（易错点，务必用这个数）：
一周期恰好推进 N 次窗口（`ph_acc += N`，`>= m_period` 推进并减 M），
故 50 Hz / N=1024 / 26 MSPS 下 **每窗 ≈508 样本、19.53 µs、角宽 0.3516°**，
窗内工频变化仅 **0.55 码** → "准直流"成立。
（**不是** 1024 ÷ 26 MSPS = 39.4 µs——那是把相位窗的**数量**当成了每窗**采样点数**。）
分窗是**自适应**的（`m_period` 取上一周期实测样本数），工频漂移时仍严格均分。

### 3.2 为什么"先滤波"是判据可用的**必要条件**（不是偏好）

判据用的是**绝对幅度门槛**（`pd_feature_core.v` L500–503 `gate = amp >= cfg_thresh`，
`amp1 = absp_min_r`，单位 ADC 码），默认 **40 码**：

| 量 | 数值 | 与门槛的关系 |
|---|---|---|
| 噪声底 σ | **7 码** | 40 码 = **5.7σ**（合理设定） |
| 工频幅值 | **90 码** | 90 码 = **12.9σ** |

不在算法里做任何频率选择时，门限比较的是 `|max|/|min|` 在**相位窗内**的幅值——
工程自带激励 `sim/generate_pd_adc_waveform.m` 为
`2048 + 90·sin(2π50t) + 18·sin(2π1.2e6t) + 7·randn`，工频瞬时值直接进入比较，
而相位窗内工频近似恒定 → **约 70% 的相位窗会越过 40 码门槛**（`90·|sinθ| ≥ 40`）。

**推论**：原始直提必须把门槛抬到 > 90 码（> 12.9σ），小放电量全部丢失 →
**灵敏度崩溃**。所以"先滤波"不是优化选项，是 40 码门槛可用的**前提条件**。

### 3.3 决策规则表（六种条件 → 方案）

| 条件 | 方案 | PL 代价 |
|---|---|---|
| 板级前端**已加**高通/带通 | 数字滤波可降到 1 节、甚至取消 | 0–8 DSP |
| 前端**无**带通，干扰轻 | **1 节**带通（8 DSP） | 8 DSP |
| 有 1 个强窄带干扰 | 1 带通 + **1 陷波**（2 节） | 16 DSP |
| ≥2 个强窄带干扰 | 1 带通 + 2 陷波（3 节） | 24 DSP |
| 需要频谱选频 | 走 **PS 侧 FFT + bin 置零**，不在 PL 加节 | 0 |
| 目标 65 MSPS | 先做 **CIC 4:1 抽取**（§九），再谈节数 | ≈700 LUT |

**已排除的四种方案（附排除理由，避免重复讨论）**

| 方案 | 排除理由 |
|---|---|
| 片内 FIR 替代 IIR | 要满足同等过渡带，阶数 ×20 量级；当前 LUT 已 67.6%，**没有 LUT 余量** |
| 卡尔曼滤波 | 非线性相位会污染 PRPD 相位；且算力远超 20 ms 预算 |
| 多相 FIR 抽取 | 吃 20 DSP，但本设计 **LUT 紧（67.6%）、DSP 松（38%）** → 应选吃 DSP 的方案 |
| 片内 4 路并行 FFT | 已在 xc7z020 上实测超容（见 §四 P0） |

### 3.4 两处必须澄清的数学事实（避免被"看起来不行"误导）

**① `CW=18 / FW=16` 精度够用**（对典型 PD 参数）：

```
Q2.16 分辨率 2⁻¹⁶，系数量化误差 ≤ 2⁻¹⁷ = 7.6e-6
二阶高通 fc=100 kHz @ 26 MHz：r = √a2 = 0.98307 → 1-r = 0.01693
  a2 量化 → r 误差 3.9e-6 → (1-r) 相对误差 2.3e-4 → fc 误差约 0.02%
陷波：Q 相对误差 = 3.8e-6 × 2Q / sinω
  @1.2 MHz、Q=100 → 0.27%；Q=380 → 1.0%
```
→ 对 PD 陷波典型 Q（10–100）**完全够用**；**精度不构成级数裁剪的障碍**。
真正需要仿真确认的是**多级级联的舍入噪声累积**（每级 `DW=16`），不是系数分辨率。

**② "1 节 biquad 做宽带带通"是可行的**（曾被误判为不可行）：

```
[100 kHz, 6 MHz] → Q = f0/BW = 3.05/6 ≈ 0.131 < 0.5
→ 两个极点落在实轴（z = 0.9754 / 0.0758），等效于「1 级 HP × 1 级 LP」
→ 每侧 6 dB/oct，但 50 Hz 处仍达 −66 dB，远超所需
```
→ **P1 的 1 节配置在功能上站得住**。0.5 < Q < 1 的窄带才需要 2 节（或级联）。
（注：Q2.16 下二阶节截止下限 ≈893 Hz，**50 Hz 本身不可表示**——所以截止频率必须由
PD 频带下限决定，而不是"用一个高通去掉 50 Hz"。）

### 3.5 目标频带与 65 MSPS 下的抽取路径

**用户已给定目标频带：100 kHz – 1 MHz。**

65 MSPS 下的正确结论不是"变慢"，而是**功能失效**：`pd_iir_biquad.v` L56–59 中 `dv` 优先级
高于 `phase == 3'd4` 回卷，65 MSPS（每样本 2 拍）时 `dv` 每 2 拍把状态机永久锁在
`phase=1`（逐拍推演 = 1,2,1,2,…），`dout_dv` 恒为 0。原生跑 65 MSPS 需 clk ≥ 325 MHz，
7020 不可达。

**路径：先抽取再滤波。**

| 项 | 结论 |
|---|---|
| 抽取比 | **D=4 是最小可行**（D=1/2 只有 2/4 拍，低于单节 5 拍门槛）→ 16.25 MSPS = **8 拍/样本，余量 1.6×** |
| 结构 | **CIC N=4**（≈700 LUT / 0 DSP）优于多相 FIR（20 DSP）——**理由是资源落点**，不是性能 |
| CIC 镜像抑制 | 混叠带 15.25–17.25 MHz 紧贴 CIC 零点（16.25 MHz 整数倍）→ **−91.7 dB**（D=8 仅 −68.4 dB） |
| droop | −0.203 dB（D=8 的 −0.857 dB 才需补偿） |
| 内部位宽 | 21 |
| `pd_filter_chain` | **零改动**（滤波逻辑只依赖 `adc_dv` 节拍） |
| `pd_feature_core` | `m_period` 自动测量（抽取后 M=162,500）；`m_cnt>1024` 与 `ph_acc` 32 位均充裕 |
| 系数精度改善 | Q16 极点分辨余量 2,763×q → **10,218×q** |
| 节数 | 抽取后 100k–1M 带通：1/2/3 节在 50 Hz 为 −66.9 / −133.9 / −200.8 dB → **推荐 2 节** |

**需同步改 12 处常量**（含 CDC 比率 5:1→2:1、只读 `0x1058`、`deadtime` 100→81、
`PD_M_DEFAULT` 400000→325000、XDC 周期 38.461538→15.384615 ns）。
**代价**：判决支路丢掉 >1 MHz 分量；CIC 群延迟 ≈10.5 输入样本（≈162 ns）需补偿；
**40 码门槛须重标定**。
**不用改**：四槽——65 MSPS 单快照 7.44 MiB < 单槽 12 MiB。

---

## 四、定案三：四阶段资源路线

| 阶段 | 动作 | 预期效果 | 前置依赖 |
|---|---|---|---|
| **P0** | **不在 PL 保留四路 FFT** | **已满足**：当前已保存 BD 不含 XFFT / adapter / bin monitor | — |
| **P1** | 每通道 IIR **8 节 → 1 节**（`N_BP=1, N_NT=0`，32 → 4 个 biquad） | DSP **84 → 28**；Slice **96.57% → 68.5–80.1%**（释放 ≈2,700–3,900） | **门禁 V0 + B0**（§六） |
| **P2** | IP 瘦身（`ila_0` depth/probe、`pd_dds_0` 自测源） | 与 P1 **可并行**，不必等 | — |
| **P3** | 实现策略调整（physopt / directive） | **必须等 P1 结论**，否则不可归因 | P1 |
| **P4** | RTL 结构级优化 | 收益最高但**需重做逐位回归**，最后做 | P1 / P3 |

**P0 的证据与约束**（定稿 §1 原文）：

- 四路 FFT 的实测代价（E-33，综合口径 Δ）：LUT-as-Logic **8,017**、LUT-as-Memory **3,404**
  （其中 SRL **3,280**）、FF **17,174**、BRAM **16**、DSP **48**、控制集 **117**；
  含 FFT 版总量 LUT 48,712（91.56%）、FF 56,147、BRAM 42.5、DSP 132，
  放置需 **7,232** slice > 可用 **7,205** → `Place 30-487` **硬失败**。
- 对照：不含 FFT 的 v1.4.0 基线 **12,646 slice（95.08%）可正常布通**。
- → **"PL 资源不足"完全由 FFT 分支引起，不是自研 RTL 的问题**；此前无法归属的
  "4,522 个 LUT-as-SRL"由此闭合。
- 三条不可违反的约束：
  1. 不新增四路并行 PL FFT；
  2. 未来若提出**单个** PL FFT，必须先有 OOC 报告**和**"单 FFT + 滤波 + 四槽管理器"的实现报告；
  3. FFT/观测支路**永远不得**对 ADC→DDR、滤波或特征提取主通路施加反压。

**P1 的账**（before 为已实现的 8 节 + 槽管理器实测值）：

| 项 | before（8 节 + 槽管理器） | after 预期（1 节） |
|---|---|---|
| Slice | 12,844 / 13,300 = **96.57%** | **9,116–10,656（68.5–80.1%）** |
| DSP | 84 | **28** |
| LUT | 36,358（68.34%） | 释放 ≈8,960 |
| FF | 39,541 | — |
| 控制集 | 1,253 | 下降（待实测） |
| WNS / TNS | **+0.007 / 0**（零余量） | 目标 ≥ +0.05 |
| 放置 | 成功（0 个 30-487），**未生成比特流** | — |

> ⚠️ 判定口径：**"资源是否够"不能用 LUT% 判断**——v1.4.0 的 LUT 只有 67.6% 而 Slice 已达
> 95.08%。必须同看 **Slice、控制集、WNS 正余量**三项。

> ⚠️ 两种等价写法：`N_BP=1,N_NT=0` 与 `N_BP=0,N_NT=1` **硬件完全相同**（`N_STAGE = N_BP+N_NT`）。
> 统一取 `1/0`，因为 P1 脚本的断言按 `1/0` 校验（`string match "*.N_BP(1)*"`）。

---

## 五、执行步骤（阶段 A–E）

| 阶段 | 动作 | 耗时 | 判据 |
|---|---|---|---|
| **A** | 地址映射门禁：`open_bd_design` + `validate_bd_design` | 1 min | 无 `BD 41-1173` / `BD 41-1356`；三条 PS 控制地址（`pd_feature_0 0x4000_0000` / `pd_ddr_0 0x4001_0000` / `pd_filter_0 0x4002_0000`，各 64K）与三条 HP DDR 映射正确 |
| **B** | 固化 8 节 **before** 证据：复用**已完成**的 `impl_1` 导出三份报告 + 备份 8 节 DCP | **约 2 min** | `util_hier_base.rpt` / `ctrl_sets_base.rpt` / `timing_*_base.rpt` 三份齐全 |
| **C** | **唯一设计改动**：`CONFIG.N_BP 2→1`、`CONFIG.N_NT 6→0` → `generate_target all` | 5 min | 生成包装模块为 `.N_BP(1) / .N_NT(0)` |
| **D** | 重综合 + 重实现，取 **after** 报告 | **50–60 min** | `P1_JOBS` 与 before 一致；策略与约束一致 |
| **E** | 验收 V1–V7 并回传 `reports/p0p1_20260917/p1_summary.txt`（**纯 ASCII**） | — | 见 §六 |

**三条硬约束**

1. **顺序必须成对**：B 必须在 `reset_run` **之前**跑完，否则为了拿 before 要多花 50 分钟。
2. **before / after 必须落在同一个工程目录**：
   `merge/pd_feature_bd_2020_2_ps_dsp_split_2026091`。
   该目录已有 **impl_1 14:52 完成**的存量证据（`utilization_placed.rpt` 14:23、
   `timing_summary_postroute_physopted.rpt` 14:52、`control_sets_placed.rpt` 14:23）。
   **跨工程树取 before/after → 结论不可归因。**
3. **P1 前后必须使用同一综合/实现策略、相同 job 数、相同约束**，并在 summary 里记录
   `P1_JOBS` 与 strategy 值。

**报告目录约定**

```text
reports/p0p1_20260917/base/        # before（8 节）
reports/p0p1_20260917/p1_1stage/   # after（1 节）
```
`util_hier_*` 带 `-hierarchical_depth 3`、`ctrl_sets_*` 带 `-verbose`、
`timing_*` 带 `-max_paths 10 -delay_type min_max`（**before/after 口径必须一致**）。

**已就绪脚本**（均在 `merge/pd_feature_bd_2020_2_ps_dsp_split_2026091/scripts/`）：
`20_p1_filter_stage_reduction.tcl`（双模式：`baseline` 复用 impl_1，约 2 min / `apply`，
约 50–60 min）｜`21_ooc_stage_sweep.tcl`（单模块 OOC 扫 N_STAGE=1/2/8，3–9 min，
**只给趋势不给全设计 Slice**，内置 `-generic` 失效自检 `OVERRIDE-SUSPECT`）｜
`tcl_stub_check.tcl`（命令桩校验，三脚本 exit=0）。

---

## 六、门禁与验收

### 门禁 B0 —— `generate_target` 之后、综合之前（1 分钟，必查）

| # | 检查项 | 通过条件 |
|---|---|---|
| ① | 4 个 module_ref 的 `<cell>.xml` 中 `dataType="integer"` | **≥ 1**（每个） |
| ② | 4 个 `<cell>.v` 中 `("[0-9]` 的命中数 | **= 0** |
| ③ | BD 网表 `pd_feature_bd.v` 中 `[0:0]` 出现次数 | **≈ 12** |
| ④ | `.xpr` 中 `$PPRDIR/../` 外部引用数 | **= 0** |

**为什么必须有这道门（不是形式主义）**：16:48 那次综合失败
（`[Synth 8-5834] Design needs 18301 RAMB18 ... > 280`，**65.4× 器件容量**）的根因就是
②③④ 同时被破坏——生成 wrapper 把参数写成字符串（`.CH_NUM("4")` → Verilog 按 8 位 ASCII
求值 0x34 = 52；`.ADC_W("12")` → 0x3132 = 12,594），导致
`o_s48_data`（设计 48 位）被撑到 654,888 位，寄存器位索引实测到 **50,376 = 4 × 0x3132**。
③ 是同一件事的第二症状：接口维度退化，真实信号被"复制拼接"（10 处，最大 64×）。
④ 指出工程仍在引用**另一棵树**的生成产物。
根因链已闭合到 16:18 那次会话：`Project file moved` → `IPUserFilesDir: Directory not found
... using path 'F:/xinya/v5/merge/...' instead` → `BD 41-2576 / 41-434`（引用的 xci 不存在）
→ `Design successfully migrated to use XCI files`（**丢 `dataType` 的就是这一步**）
→ `BD 41-1356` ×3（从属地址未分配）。

> **结论**：B0 不是可选项。**②③④ 任一命中即停**，先修工程树再综合。
> ⚠️ 另：`pd_filter_0` 与 `pd_dds_0` 的 wrapper 是**裸值**（正确），所以"把 BD 改回 2/6
> 重跑 35 分钟"的分岔实验**作废，请勿执行**——白跑且会再失败一次。

### 验收 V1–V7

| # | 项 | 通过条件 |
|---|---|---|
| **V1** | 放置 | 0 个 `Place 30-487` |
| **V2** | Slice | **≤ 85%**（发布目标） |
| **V3** | 时序 | **WNS ≥ 0 且 TNS = 0**（发布目标 WNS ≥ +0.05 ns） |
| **V4** | **旁路态逐位回归** | 第一道闸门：默认旁路下 `pd_filter_0` 输出与输入**逐位一致** |
| V5 | DDR 主通路 | 完整性未破坏（环形写 / 冻结 / 四槽） |
| V6 | 控制集 | 记录 `report_control_sets`（基线 1,253） |
| V7 | 可复现性 | 记录 `P1_JOBS` 与 synth/impl strategy |

> **V4 为什么是第一道闸门**：滤波复位后全局旁路与四通道旁路均为 1，P1 前后默认输出**都**是
> 原始数据直通。因此**不需要系数**就能做逐位回归——零成本、可判定，是最划算的验证。

### 回退

8 节 DCP 在阶段 B 已备份 → 回退只需还原 BD 的 `N_BP/N_NT` 并复用该 DCP，不必重跑综合。

---

## 七、契约修订清单

| 条款 | 现状 | 建议修订 | 理由 |
|---|---|---|---|
| **§2 L38** | "DDR 接收**滤波后**数据" | 改为 **"DDR 收原始 ADC"** | 与实现不符（实现存原始）；且 PS 迁移**要求**快照为原始数据 |
| **§2 L42** | "滤波由 PL 实时执行，PS 只负责计算下发送系数" | 增补分工：PL 只做**实时判据滤波**；**FFT/选频移 PS** | ⚠️ **这是契约职责反转，需双方确认** |
| **§8 L244 / L267** | "不承诺 Biquad 数量、定点格式、频带或资源占用" | 定案后**明确写入**：级数、Q2.16、带通/陷波频点 | 当前表述过于宽泛，无法作为验收基线 |
| **新增** | — | **显式记录 `pd_feature_0/adc_data` 连线取向为"串联"** | 防止将来被顺带改成并联而不留痕（并联就是这么发生的） |

**未定前提（O1）**：板级模拟前端**是否加高通/带通**（契约 §2 L34 标注"硬件方向 TBD"）。
**会加** → 并联更优、数字滤波可简化；**不会加** → 串联/混合是必要条件。
→ **定案前不要动那条连线。**

---

## 八、待决策清单

| # | 问题 | 需谁决定 | 影响 |
|---|---|---|---|
| **O1** | **板级前端会不会加高通/带通**（§2 L34 "硬件方向 TBD"） | 用户 + 硬件方向 | **最关键**：决定拓扑方向与数字滤波是否必要 |
| O2 | 滤波节数最终取 1 还是 2 | 由实测频谱决定 | 节数 = 1 + 实测强窄带干扰个数 |
| O3 | 陷波选哪几个频点 | 需实测频谱支撑 | **不能凭猜** |
| O4 | 是否补成混合拓扑（§2.6） | 用户 | 取决于频谱功能定案 |
| O5 | 契约 §8 的级数与频带是否写入 | 双方（含 PS 侧搭档） | 契约是双方确认基线 |
| O6 | 契约 §2 L38 与实现不符（DDR 收原始还是滤波后） | 双方 | 应改契约为"收原始" |
| **N1** | 100k–1M 是**通带指标**还是大致范围 | 用户 | 决定 CIC 的 N=3/4 与节数 2/3 |
| N2 | >1 MHz 是干扰还是信号 | 用户 | 决定抽取后判决支路丢带是否可接受 |
| N3 | 40 码门槛是否重标定 | 用户 | 抽取 + 滤波两级都会改变幅度口径 |
| N4 | 65 MSPS 是四通道同时还是分级 | 用户 | 决定是否够 8 拍/样本余量分配 |
| N5 | 前端是否已加高通/带通（同 O1） | 用户 | 同 O1 |

**已关闭的待一致事项**：`N_BP/N_NT` 语义｜地址公式｜APPLY 握手｜相位窗时长｜
50 Hz 高通不可表示｜E7 保护改位图｜E8 mute 用 `adc_dv` 计数。

---

## 九、明确不做 / 已知不解决

**本期明确不做**

1. 不新增四路并行片内 FFT（v1.5.0 已归档，标注"实现失败、**不能用于 bitstream 或上板**"）；
2. 不做片内**单** FFT——除非先有 OOC 报告 + "单 FFT + 滤波 + 四槽管理器"实现报告；
3. **P1 期间**：不生成发布 bitstream / XSA、不推 GitHub、**不启用滤波**、不取消旁路、
   不回退 E-31 的 RTL 改动、不删除 `ila_0` / `pd_dds_0`；
4. 本期**不设计滤波系数**、不改数据连线、不改 DDR 四槽管理器、不改 PS 软件 API；
5. 不执行"把 BD 改回 `N_BP=2/N_NT=6` 重跑分岔实验"（见 §六 B0 结论）。

**已知但不解决的问题**

| 项 | 状态 | 说明 |
|---|---|---|
| **65 MSPS** | 本方案不解决 | `pd_iir_biquad` 5 周期/样本 → 65 MSPS 只有 2 拍 → **功能失效**（非变慢）。路径 = CIC 4:1 抽取（§3.5），需独立立项 |
| **E-31 三处 RTL 改动** | **P1 期间不动** | Codex 当时以"Vivado 把整数参数写成引号字符串不成立"为由改了 3 个文件（`pd_dds_adc_source.v` 删 `initial $error`、`pd_filter_chain.v` 端口位宽硬编码 + `dec_param`、`pd_feature_sys_top.v` 位宽 + `CFG_*`）。**该现象现已被实测证实真实存在**（§六 B0），但只发生在生成产物树被破坏时 → **P1 后单列清理**；被删的 `initial $error` 自检块建议恢复 |
| **`dec_param` 缺陷** | 已知，当前无害 | `pd_filter_chain.v` L45 只认 0–9，真整数 ≥10 时返回 0；当前参数下不触发 |
| **滤波系数** | **完全空白** | 全工程无任何滤波系数设计文件；频带与系数**从未设计**，效果**无法评估** |
| **溢出/饱和上报** | 未确认 | `pd_iir_biquad` L113 有饱和限幅 `yc`，是否引出标志未确认；建议补粘滞上报 |
| **层级单独占用** | 待补 | `ila_0` / `pd_dds_0` / DataMover 的层级占用两版报告均未给出；`report_utilization -hierarchical` 已进 P1 脚本 |
| **控制集** | 加剧因素非唯一根因 | 基线 1,253 下仍能放置 → 控制集不是唯一根因 |

---

## 十、一句话总结

> **数据流**：一次采集、单点扇出、三支并行——扇出点已存在于 `pd_ddr_wr_top.v` 内，
> **唯一要改的是把分析支路从"滤波之后 + PL 之内"换到"原始快照 + PS 侧"**。
>
> **滤波**：先定带再选构——工频交给相位窗（0 硬件），频带交给 **1~2 节 IIR**（8~16 DSP），
> 频谱交给 **PS 侧 FFT bin 置零**（0 PL 资源）。
>
> **资源**：P0（不保留四路 FFT）已满足；**P1 是本期唯一设计改动**——每通道 8 节 → 1 节，
> DSP 84 → 28，Slice 96.57% → 68.5–80.1%。
>
> **门禁**：综合前必过 **B0 四项**（16:48 的 18,301 RAMB18 就是没过这道门的代价）；
> P1 前必过 **V0**；发布前必过 **V1–V7**，其中 **V4 旁路态逐位回归是第一道闸门**。
