# 局部放电采集、特征提取与 DDR 接口契约 v3.0

文档状态：**双方确认基线；硬件资料相关项仍为 TBD**  
生成日期：2026-09-10  
目标工程：`F:\xinya\v4\pd_feature_bd_2020_2`  
工具版本：Vivado 2020.2  
目标器件：XC7Z020-2CLG400  
当前支持采样率：26 MSPS

> 本文档由接口契约 v1.1、当前主工程、上传的 `pd_feature` v3 RTL，
> 以及本轮已确认的合并决策整合而成。B 方已于 2026-09-10 确认本契约，
> APPLY_STATUS 差异也已由 A 方定案。“拟实现”内容须在 RTL/BD 修改和
> 回归验证完成后才能转为“已实现”。

## 1. 范围与状态

本契约定义四通道 ADC 采集、统一 CDC、特征提取、DDR 存储、AXI-Lite
寄存器、AXI-Stream 事件流，以及未来 PL 实时滤波模块的接口边界。

| 标记 | 含义 |
|---|---|
| 已实现 | 当前 v4 主工程已有代码证据 |
| 拟实现 | 已确认纳入本轮，但尚未完成合并或验证 |
| 预留 | 只冻结接口或地址，不实例化功能模块 |
| TBD | 仍需对方或硬件资料确认 |

本版不声明数字带通、六组陷波、FFT、质量事件、六 beat 统计包、PS 驱动、
TCP 上传或上板联调已经完成。ADC 时序资料不足时也不声明 I/O 已最终签核。

## 2. 数据流与职责

```text
ADC 4ch x 12bit @ 26 MSPS
  -> 板级采样接口/adc_dv适配（保持现状，硬件方向 TBD）
  -> 48bit统一异步FIFO CDC（BD顶层唯一CDC所有者）
  -> [预留] pd_filter_chain
  -> pd_feature_0 -> AXI4-Stream -> AXI DMA -> PS/DDR
  -> pd_ddr_0（并行接收滤波后数据，环形写入/冻结快照）
```

- `pd_feature_0` 与 `pd_ddr_0` 不得再次对同一数据执行异步 CDC。
- 未来滤波由 PL 实时执行，PS 负责计算、选择并下发系数。
- 本轮仅预留滤波接口和地址，不实例化空壳滤波 IP。
- DDR 冻结控制只属于 `pd_ddr_0`。

## 3. 时钟、采样与复位

| 时钟 | 频率 | 用途 | 状态 |
|---|---:|---|---|
| `adc_clk` | 26 MHz | ADC采样与统一CDC写侧 | 接口保留，方向待板级确认 |
| `clk130m` | 130 MHz | 特征、DDR、AXIS与内部处理 | 保持 |
| AXI控制时钟 | 由BD连接确定 | AXI4-Lite控制 | 已实现 |

当前版本只支持 26 MSPS，65 MSPS 不属于 v3.0 已支持或已验证能力。

| 26 MSPS固定参数 | 数值 |
|---|---:|
| 50 Hz周期采样点数 | 520000 |
| 5 us死区采样点数 | 130 |
| 处理时钟与采样率之比 | 5:1 |

所有时间参数必须集中按 26 MHz 换算，不得继续使用 20 MSPS 对应的
`400000` 等历史常量。

当前保留 `adc_clk` 输入、48 bit `adc_data` 输入、4 bit `adc_dv` 输入和
`sync_in` 输入。在获得原理图、ADC最终型号、采样边沿和板级时序前，
不得删除 `adc_dv`、改变时钟方向或猜测 input delay。

复位规则：外部复位可异步置位，但必须在各目标时钟域同步释放；软件复位
必须转换为同步单拍脉冲；禁止组合逻辑直接驱动大规模寄存器、DSP或BRAM
的异步清零端。

## 4. PS AXI 地址映射

| AXI从设备 | PS基地址 | 地址段 | 用途 | 状态 |
|---|---:|---:|---|---|
| `pd_ddr_0` | `0x40000000` | 64 KB | 环形缓存与冻结控制 | 已实现 |
| `pd_feature_0` | `0x40010000` | 64 KB | 特征、统计与PRPD | v3拟扩展 |
| `pd_filter_0` | `0x40020000` | 64 KB | PL滤波控制，首个4 KB使用 | 预留 |
| `axi_dma_0` | `0x40400000` | 64 KB | DMA控制 | 已实现 |

软件必须使用“从设备基地址 + 页内偏移”。AXI地址段按64 KB分配，设备
内部功能页按4 KB对齐。

## 5. `pd_feature_0` 寄存器

### 5.1 页划分

| 页内范围 | 用途 |
|---|---|
| `0x0000-0x0FFF` | 通道配置与状态 |
| `0x1000-0x1FFF` | 全局控制、版本与六档门限 |
| `0x2000-0x2FFF` | 十二档计数与帧快照 |
| `0x3000-0x3FFF` | 保留，读零、写忽略 |
| `0x4000-0x7FFF` | 四通道PRPD RAM窗口 |

### 5.2 通道寄存器

通道 `i` 页内基址为 `i * 0x80`，PS绝对地址为
`0x40010000 + i * 0x80 + local_offset`。

| 偏移 | 名称 | 类型 | 主要字段或单位 |
|---:|---|---|---|
| `0x00` | `CTRL` | RW/W1P | enable、sw_rst、auto_clear、ev_all、prpd_max、prpd_clear |
| `0x04` | `CFG0` | RW | mode与phase_win，有效1..2048 |
| `0x08` | `PHASE_INC` | RW | 32 bit相位累加步长 |
| `0x0C` | `THRESH` | RW | 基础事件门限，ADC码 |
| `0x10` | `SCALE` | RW | Q8.8，ADC码到pC标定系数 |
| `0x14` | `UPEAK` | RW | Q8.8试验电压峰值 |
| `0x18` | `DEADTIME` | RW | 死区采样点数，5 us默认130 |
| `0x1C` | `MEAS_CYC` | RW | 测量帧同步周期数，0表示不自动快照 |
| `0x20` | `STATUS` | RO | sync_locked、meas_done、overflow |
| `0x24` | `SYNC_PER` | RO | 实测同步周期，单位ADC样本 |
| `0x28` | `CNT_N` | RO | 累积事件数 |
| `0x2C` | `Q_MAX` | RO | 最大视在电荷量，Q8.8 pC |
| `0x30/0x34` | `SUM_ABS_LO/HI` | RO | sum(abs(q)) |
| `0x38/0x3C` | `SUM_QU_LO/HI` | RO | sum(q*u) |
| `0x40` | `LIVE_CYC` | RO | 累积同步周期数 |
| `0x44` | `IRQ_STAT` | W1C | meas_done与overflow |
| `0x48` | `IRQ_EN` | RW | 对应中断使能 |
| `0x4C-0x60` | `REG0/REG1_*` | RW | 两个PRPD剔除区域 |

### 5.3 全局寄存器与六档门限

| 页内偏移 | 名称 | 类型 | 说明 |
|---:|---|---|---|
| `0x1000` | `GCTRL` | RW/W1P | global_enable、global_sw_rst |
| `0x1004` | `GSTATUS` | RO | 四通道同步锁定状态 |
| `0x1008` | `GIRQ_STAT` | W1C | 通道中断聚合状态 |
| `0x100C` | `GIRQ_EN` | RW | 全局中断使能 |
| `0x1010` | `VERSION` | RO | `0x00030000` |
| `0x1014` | `NUM_CH` | RO | 固定为4 |
| `0x1018` | `FIFO_CNT` | RO | 输出FIFO占用量 |
| `0x101C` | `EV_CNT_LO` | RO | 已发送AXIS beat计数低32 bit |
| `0x1020` | `EV_CNT_HI` | RO | 已发送AXIS beat计数高32 bit |
| `0x1024-0x1038` | `POS_THR[0..5]` | RW shadow | 六档正门限，16 bit ADC码 |
| `0x1040-0x1054` | `NEG_THR[0..5]` | RW shadow | 六档负门限，16 bit ADC幅值 |
| `0x1058` | `SAMPLE_RATE_HZ` | RO | 拟实现，固定返回26000000 |
| `0x105C` | `APPLY_STATUS` | RO | 拟实现，字段已冻结 |
| `0x1060` | `SHADOW_APPLY` | W1P | bit0写1提交门限配置 |

六档门限由四通道共用，按幅值从低到高排列。PS写shadow配置后，通过
`SHADOW_APPLY` 提交。每个通道在自己的下一同步周期边界更新active
配置；不保证四通道同拍切换，但单个通道的一帧内不得混用新旧门限。

`APPLY_STATUS` 最终字段如下：

| 位 | 名称 | 含义 |
|---:|---|---|
| bits3:0 | `pending_ch` | 每通道待应用标志；bit i对应通道i |
| bits7:4 | `applied_ch` | 每通道已应用标志；bit i+4对应通道i |
| bits15:8 | `reserved` | 保留，读零 |
| bits31:16 | `apply_seq` | 四通道全部应用成功后自增，16 bit自然回绕 |

### 5.4 十二档计数与帧快照

通道 `i` 的计数页基址为 `0x2000 + i * 0x80`。

| 偏移 | 名称 | 类型 | 说明 |
|---:|---|---|---|
| `0x00-0x14` | `POS_CNT[0..5]` | RO | 六档正事件计数 |
| `0x18-0x2C` | `NEG_CNT[0..5]` | RO | 六档负事件计数 |
| `0x30` | `FRAME_N_POS` | RO | 已发布帧的正事件合计 |
| `0x34` | `FRAME_N_NEG` | RO | 已发布帧的负事件合计 |
| `0x38` | `FRAME_AD_MAX` | RO | 有符号16 bit ADC最大码 |
| `0x3C` | `FRAME_AD_MIN` | RO | 有符号16 bit ADC最小码 |
| `0x40` | `FRAME_ID` | RO | 32 bit递增帧号 |
| `0x44` | `FRAME_STAT` | RO | bit0 valid，bit1 overflow |
| `0x48` | `FRAME_ACK` | W1P | 拟实现：bit0确认已读，bit1清overflow |

- `FRAME_N_POS/NEG` 统计通过基础门限、死区和剔除规则后的事件。
- 档位采用累计门限语义：事件达到第 k 档门限时，对应档计数增加。
- `FRAME_AD_MAX/MIN` 单位为有符号ADC码，不是Q8.8 pC。
- 十二档计数、帧合计、AD极值和 `FRAME_ID` 必须来自同一发布帧。
- 禁止通过读取 `FRAME_STAT` 自动清除状态。

PS读取流程：先读 `FRAME_STAT`；读取 `FRAME_ID=id0`；读取全部快照；
再次读取 `FRAME_ID=id1`；若两者不同则重读；一致后写
`FRAME_ACK[0]=1`。若overflow为1，记录丢帧并写 `FRAME_ACK[1]=1`。

### 5.5 PRPD窗口

通道 `i` 页内基址为 `0x4000 + i * 0x1000`。每通道包含1024个
16 bit只读PRPD值；AXI按32 bit读取，低16 bit有效。

## 6. `pd_ddr_0` 快照与冻结接口

| 偏移 | PS绝对地址 | 名称 | 类型 | 语义 |
|---:|---:|---|---|---|
| `0x20` | `0x40000020` | `SNAP_BASE` | RW | 快照目标DDR基址，必须位于编译期快照分区且24字节对齐 |
| `0x24` | `0x40000024` | `SNAP_SIZE` | RW | 快照目标区域大小，必须为24字节整数倍 |
| `0x28` | `0x40000028` | `FREEZE_BASE_LO` | RO | 已冻结原始数据起始地址低32 bit |
| `0x2C` | `0x4000002C` | `FREEZE_BASE_HI` | RO | 地址高32 bit；当前32 bit实现固定返回0 |
| `0x30` | `0x40000030` | `FREEZE_LEN_BYTES` | RO | 本次冻结数据长度，单位byte |
| `0x34` | `0x40000034` | `FREEZE_CTRL` | W1P/RO | 冻结、恢复命令及快照状态，位域见下表 |
| `0x40` | `0x40000040` | `SNAP_CHUNKS` | RO | 已完成的快照拷贝分片数 |
| `0x44` | `0x40000044` | `SNAP_BYTES` | RO | 本次已完成的快照拷贝字节数 |
| `0x48` | `0x40000048` | `SNAP_SEQ` | RO | 每次成功完成快照后自增，自然回绕 |

`FREEZE_CTRL` 写语义：

| 位 | 写入语义 |
|---:|---|
| `0` | 写1产生一个时钟周期的冻结触发脉冲 |
| `1` | 写1产生一个时钟周期的解除冻结脉冲 |
| 其他 | 保留，写入无效 |

`FREEZE_CTRL` 读回语义：

| 位 | 名称 | 含义 |
|---:|---|---|
| `0` | `ACQ_EN` | 当前采集使能状态 |
| `8` | `FREEZE_DONE` | 冻结描述符已形成 |
| `9` | `COPY_BUSY` | 快照拷贝进行中 |
| `10` | `COPY_DONE` | 快照拷贝完成，粘滞状态 |
| `11` | `COPY_PENDING` | 自动快照正在等待写尾落地或拷贝资源 |
| `12` | `SNAP_OVERRUN` | 快照请求发生覆盖/拥塞 |
| `13` | `COPY_ERR` | 快照拷贝错误 |
| `14` | `SNAPSHOT_CFG_ERR` | 快照地址、长度或24字节对齐配置错误 |
| 其他 | `RESERVED` | 固定返回0 |

`FREEZE_CTRL` 的写命令位和读状态位是非对称接口，软件不得通过读改写
方式提交命令。冻结与快照寄存器只属于 `pd_ddr_0`。旧文档中将其
描述为特征页 `0x40011028-0x40011038` 的内容作废。

## 7. AXI4-Stream事件协议

总线宽度固定64 bit。`TVALID=1 && TREADY=0` 时，`TDATA` 和 `TLAST`
必须保持稳定。

峰值事件 `type=0x00` 保持原二进制布局：`type[63:56]`、
`q[55:40]`、`phase[39:28]`、`polarity[27]`、`ch_id[26:25]`、
`event_seq[24:0]`。

周期统计事件 `type=0x01`：`type[63:56]`、`cycle_idx[55:32]`、
`ch_id[31:30]`、`cyc_n[29:16]`、`cyc_qmax[15:0]`，单beat且
`TLAST=1`。由于两种包的 `ch_id` 位置不同，PS必须先按type选择解析格式。

`type=0x04` 质量事件与六beat完整统计包本轮仅预留。接收端遇到未知
type时必须按 `TLAST` 跳过，不得使后续事件流失步。

## 8. PL滤波模块预留

滤波职责为四通道26 MSPS实时带通和陷波，PS负责系数计算和下发。

```verilog
module pd_filter_chain #(
    parameter integer NUM_CH    = 4,
    parameter integer ADC_W     = 12,
    parameter integer CLK_HZ    = 130000000,
    parameter integer SAMPLE_HZ = 26000000
)(
    input  wire                    clk,
    input  wire                    rst_n,
    input  wire [NUM_CH*ADC_W-1:0] adc_data,
    input  wire [NUM_CH-1:0]       adc_dv,
    output wire [NUM_CH*ADC_W-1:0] filt_data,
    output wire [NUM_CH-1:0]       filt_dv
    // 独立AXI4-Lite接口待实现
);
```

- PS基地址预留为 `0x40020000`，64 KB地址段，首个4 KB用于控制和系数。
- 支持全旁通；旁通时位宽、通道顺序和 `adc_dv` 语义保持不变。
- 系数使用shadow/active双组；切换边界和IIR状态策略在实现前确认。
- 模块位于统一CDC之后、特征提取和DDR写入之前。
- 当前不承诺Biquad数量、定点格式、频带或资源占用。

## 9. 访问、版本与兼容规则

- 寄存器按32 bit对齐访问；W1P写1产生单拍脉冲；W1C写1清状态。
- 64 bit计数器使用高-低-高重读法，或未来统一快照机制。
- 保留地址读返回0、写忽略；是否返回SLVERR留待后续版本决定。
- AXI读数据须保留主工程已有的从机边界寄存流水，避免大读mux成为关键路径。
- `VERSION[31:16]` 为主版本，`VERSION[15:0]` 为次版本。
- v3版本值为 `0x00030000`。
- PS启动时必须读取 `VERSION`、`NUM_CH` 和 `SAMPLE_RATE_HZ`。
- 不支持主版本3的软件必须拒绝启动采集，不得按旧地址静默运行。

## 10. 验证与验收

模块级回归至少覆盖六档门限、shadow/apply、逐通道周期边界生效、十二档
计数、帧一致性、显式ACK、覆盖overflow、AXIS反压和v2/v3峰值包逐bit兼容。

系统级验收要求：

- 验证48 bit统一CDC后的四通道样本对齐。
- 验证事件经过DMA进入DDR的格式和顺序。
- 验证DDR与特征寄存器地址互不混用。
- 必须在 `xc7z020clg400-2`、Vivado 2020.2主工程重新回归。
- 历史 `xc7z020clg484-2` 结果只作参考。
- 干净实现目标：`WNS >= +0.2 ns` 且 `TNS = 0`。
- TIMING-2、TIMING-4、TIMING-24及关键CDC告警须清除或有书面豁免。

最终ADC I/O签核仍需ADC型号与速度等级、tCO min/max、采样边沿、原理图、
FPGA pin map、Bank VCCO及PCB时钟/数据skew。

## 11. 双方确认结论

1. 主版本升级为v3.0，`VERSION=0x00030000`。
2. 六正六负门限由四通道共用。
3. 各通道在自身下一周期边界应用门限，而非四通道同拍切换。
4. `FRAME_STAT` 只读，通过 `FRAME_ACK` 显式确认和清错。
5. `SAMPLE_RATE_HZ=26000000`；`APPLY_STATUS` 采用 `pending_ch[3:0]`、`applied_ch[3:0]` 和 `apply_seq[15:0]`。
6. `type=0x00` 峰值包保持二进制兼容。
7. 质量事件和六beat统计包本轮仅预留。
8. 滤波由PL执行、PS下发系数，并预留 `0x40020000` 地址段。
9. 当前版本只支持26 MSPS，65 MSPS不属于本版能力。
10. 对方提供ADC与板级资料后再完成ADC I/O时序签核。
11. `apply_seq` 仅在四通道全部应用本次shadow配置后自增；PS以序号变化确认整次提交完成。

## 12. 相对v1.1的主要变更

1. 接口主版本升级为v3.0。
2. 固化26 MSPS、520000点/50 Hz周期和130点/5 us死区。
3. 新增六正六负门限、shadow apply和apply状态。
4. 新增十二档计数和一致性帧快照页。
5. 帧状态由读取即清除改为显式确认。
6. 明确冻结寄存器唯一归属DDR页。
7. 明确BD顶层48 bit FIFO是唯一ADC数据CDC所有者。
8. 保持旧峰值事件包兼容，新增事件按type扩展。
9. 预留PL滤波接口和 `0x40020000` AXI地址段。
10. 增加主工程重新回归及 `WNS >= +0.2 ns` 验收要求。
