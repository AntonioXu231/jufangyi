# v1.6.0-p1-resource-reduction

## 基本信息

| 项目 | 内容 |
|---|---|
| 版本号 | `v1.6.0` |
| 分支 | `feature/v1.6.0-p1-resource-reduction` |
| 日期 | 2026-09-20 |
| 作者 | AntonioXu231 / Codex |
| 变更类型 | 删除 / 修改 / 新增 / 文档 |

## 为什么要改

`v1.5.0` 引入的四路 1024 点 PL FFT 实验导致实现阶段放置失败（`Place 30-487`），确认 Zynq-7020 的 PL 无法同时承载四路 FFT 与既有实时判决链路。据此在最终定稿中做出架构裁决：**FFT 固定在 PS 侧执行，输入取自 DDR 原始 ADC 快照；PL 只保留实时判决支路与实时 PRPD**。本版本据此移除 PL FFT 实验链，并对 `pd_filter_0` 执行 P1 降载，同时补齐事件自动快照触发与 PS 侧 DDR 错误判据。

## 改了什么

| 文件或模块 | 新增/修改 | 内容摘要 |
|---|---|---|
| `pd_filter_0` BD 参数 | 修改 | P1 降载：`N_BP` 2→1、`N_NT` 6→0。综合 DSP 84→28。 |
| `pd_fft_input_adapter_4ch.v` | 删除 | v1.5.0 的四路 FFT AXI-Stream 适配，随 PL FFT 一并移除。 |
| `pd_fft_bin_monitor_4ch.v` | 删除 | v1.5.0 的每路单 bin 幅值监测模块。 |
| `xfft_ch0_3` / `_4` / `_5` 等 XCI | 删除 | 四路定点 XFFT IP 配置。 |
| `tb_pd_fft_chain.v`、`tb_pd_fft_chain_wave.tcl` | 删除 | 四路 FFT 专用行为仿真与波形集。 |
| `pd_snapshot_trigger.v` | 新增 | 事件自动快照触发逻辑。 |
| `pd_ddr_wr_top.v`、`pd_ddr_axil.v` | 修改 | 接入事件触发；新增 `0x5C SNAP_TRIG_CTRL`、`0xA0 SNAP_TRIG_DROPS`，均在既有地址表末尾之后，不改动现有偏移。 |
| `pd_ddr_bd_adapter.v`、`pd_feature_top.v`、`pd_feature_sys_top.v` | 修改 | 配合事件触发与 `event_accept` 回连的端口接线。 |
| `pd_filter_chain.v` | 修改 | P1 参数适配。 |
| `tb_pd_snapshot_trigger.v` | 新增 | 自动快照触发的模块级行为仿真。 |
| `scripts/09_remove_fft_chain.tcl` | 新增 | 移除四路 PL FFT 的可复现脚本。 |
| `scripts/10_enable_event_auto_snapshot.tcl` | 新增 | 使能事件自动快照的可复现脚本。 |
| `sim/coef_fs26m.*`、`coef_fs16p25m.*`、`coef_fs65m.*`、`gen_iir_coef.m` | 新增 | 三种采样率下的系数与 MATLAB 生成脚本（`fs65m`/`fs16p25m` 为后续 65 MSPS 抽取架构预研用）。 |
| `sw/pd_snapshot_poll.c` | 新增 | PS 侧自动快照冒烟测试，轮询方式验证 PL/DDR/AXI-Lite 契约，并纳入 `DDR_STATUS[5]` 错误判据。 |
| `sw/pd_filter_apply.c` | 新增 | PS 侧滤波器系数下发与回读验证程序。 |
| `最终定稿_数据流与后续完整实施方案_中文版_20260918.md` 等 8 份 | 新增 | 架构定稿、逐条评审、PS 代码讲解、工程状态核查与后续路线文档。 |
| `pd_feature_bd.bd`、`.xpr`、`.xsa` | 修改 | 承载上述 BD 与 RTL 变更。 |

## 不改什么

- 不改变 `pd_feature_0` 的相位窗、门槛、PRPD 与事件包接口契约。
- 不改变 `pd_ddr_0` 的原始环形写、手动 `SNAP_START`、HP0/HP1 映射与既有 AXI4-Lite 地址；新增寄存器位于原表末尾之后。
- 不改变 DDR 1 GB 可见范围、时钟频率、CDC 结构与真实 ADC 引脚约束。
- 不恢复片内 FFT（长期约束：FFT 与选频由 PS 承担）。

## 影响与兼容性

- AXI/寄存器接口：`pd_ddr_0` 新增 `0x5C`、`0xA0` 两个偏移，既有偏移语义不变；`pd_filter_0` 的寄存器不受 P1 影响。
- 时钟、复位、CDC：无变更。
- 仿真激励或预期结果：新增 `tb_pd_snapshot_trigger`；`tb_pd_fft_chain` 随功能移除而删除。
- 上板风险：中。当前 bitstream/XSA 含 `dbg_hub`/ILA（`timing_summary_routed.rpt` 中 1,311 处引用），且时序未收敛，仅可作为调试版本使用。

## 验证证据

| 验证项 | 命令或操作 | 结果 | 证据位置 |
|---|---|---|---|
| 行为仿真 | Vivado 2020.2 运行 `tb_pd_snapshot_trigger`、`tb_pd_filter_chain` | 通过 | `xsim` 日志：`TB_PD_SNAPSHOT_TRIGGER_PASS`、`TB_PD_FILTER_CHAIN_PASS` |
| 综合 | `synth_1` | 通过 | Slice LUT 26,048、DSP 28、RAMB36 22、RAMB18 9；零 ERROR、零 CRITICAL WARNING |
| 实现 | `impl_1` | 完成，但时序未收敛 | `timing_summary_routed.rpt`：WNS `-0.054 ns`、TNS `-0.491 ns`（19/91,518 端点）；WHS `+0.036 ns`、THS `0.000 ns`；`Place 30-487` = 0；布线错误 0 |
| 上板 | PS `pd_snapshot_poll` 串口输出 | **未通过** | `SNAPSHOT_FAIL: DDR ring err sticky (DDR_STATUS[5])`；`DDR_STATUS=0x00000021`、`SLOT_STATUS=0x00000000`、`TRIG=0x0001001F` |
| 顶层快照闭环仿真 | — | 未执行 | 不得据此版本声明已完成 |
| DMA S2MM / `m_axis` TREADY 门禁 | — | 未执行 | 同上 |
| 滤波旁路态与滤波态 A/B 标定 | — | 未执行 | 同上 |
| PS FFT 与频谱服务、四槽长期轮转、GIC 中断服务 | — | 未执行 | 同上 |
| 65 MSPS CIC 抽取架构、真实 AD9226、上位机 | — | 未执行 | 同上 |

## 未定位问题

上板失败状态 `DDR_STATUS=0x00000021` 仅置位 bit0(`acq_en`) 与 bit5。经 RTL 复核，`pd_ddr_wr_top.v:743` 将四路信号或运算后送入 bit5：

```verilog
.i_err (ring_err | ring_ovf | copy_err | snapshot_cfg_err),
```

实测 bit8(`copy_err`) 与 bit9(`snapshot_cfg_err`) 均为 0，故 bit5 实际等于 `ring_err | ring_ovf`（`ring_err` 为 DataMover 错误，`ring_ovf` 为上游 FIFO 溢出丢数）。现有 AXI4-Lite 寄存器表未暴露区分两者所需的信息：

- `pd_ddr_wr_top.v:453-454` 的 `o_last_btt`、`o_sts_tdata` 悬空未接。
- `dbg_ddr`（`pd_ddr_wr_top.v:782-791`，含 `ring_err[9]`）在 `.bd` 中仅有端口声明，无网络连接，无法观测。

因此该错误的根因**尚未确定**，不得写成已修复或已解释。

## 回退方法

本版本为实验归档。后续工作应从 `v1.5.0` 标签或 `main`(`f034311`) 创建新目录，而非删除本目录或改写既有标签：

```powershell
git archive v1.5.0 | tar -xf - -C <new-directory>
```
